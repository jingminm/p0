{
    "configurations": [
    {
        "type": "lldb",
        "request": "launch",
        "name": "Launch",
        "program": "${workspaceFolder}/project0_debug",
        "args": [],
        "cwd": "${workspaceFolder}"
    }
    ]
}